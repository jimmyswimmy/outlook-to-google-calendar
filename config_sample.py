# outlook
previous_days = 3  # retrieve this many past days of events
future_days = 7  # retrieve this many future days of events

# google
google_creds_path = "./credentials/credentials.json"
google_token_path = "./credentials/google_token.json"
google_calendar_id = "primary" #think this is the default otherwise change it to the calendar you want to write

# misc
events_ts_json_path = "./events_ts.json"
system_tz_offset = 4
pause = 0.1
force = False  # force full run even if no changes
